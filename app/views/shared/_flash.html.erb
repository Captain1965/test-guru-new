<div class="row justify-content-center">
  <% flash.each do |key, message| %>
    <div class="col-12 alert">      
      <%= flash_message(key) %>       
  <% end %>
  <% if flash['url'] != nil %>
    <a href=<%= flash['url'] %> target="_blank"> <%= t('.show') %></a>
  <% end %>  

</div>
