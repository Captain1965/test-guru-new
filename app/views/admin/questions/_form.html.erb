<h3><%= question_header(@question) %></h3>
  <%= form_with model: [:admin, @test, @question], local: true do |form| %>
<p>
  <%= form.label :body %>
  <%= form.text_field :body, size: 60 %>
</p>
<p><%= form.submit %></p>
  <%= link_to 'Back', admin_tests_path %>
<% end %>
